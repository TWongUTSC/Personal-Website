/*
load levels into objects
    read json
    parse json into collection of objects
    each object should have walls and size

play each level
    loop every object
    render the screen
    allow input
    keep track of current and filled positions
    check win/lose conditions after every move
*/
/*
gridGameConfig = getJSONObject("https://raw.githubusercontent.com/TerryCLAWong/Personal-Website/grid-game/gridGameConfig.json")
levels = gridGameConfig.levels

for (index = 0; index < levels.length; index++) {
    while (!playLevel(levels[index])) {
        //level loss
    }
    //level win
}
*/

//game win





